{
  "name": "aaditi-yogalaya",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "npm run build && wrangler pages dev dist --ip localhost --port 8788",
    "build": "tsc -b && vite build",
    "deploy": "wrangler pages deploy dist --project-name=aaditi-yogalaya --commit-dirty=true",
    "pages:deploy": "npm run deploy",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "wrangler": "^4.123.0"
  }
}
